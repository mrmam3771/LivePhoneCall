[project]
name = "qwen3-asr-realtime"
version = "0.1.0"
description = "Local real-time speech-to-text with Qwen3-ASR"
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "langchain",
    "langchain-anthropic>=1.0.4",
    "langchain-google-genai>=4.3.5",
    "langchain-openai",
    "modelscope>=1.39.1",
    "qwen-asr[vllm]",
]

[tool.uv.sources]
qwen-asr = { path = "Qwen3-ASR", editable = true }
